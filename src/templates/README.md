Place your .docx templates here.

Default template filename expected by the API route is:
- berita-acara.docx

You can pass a different template filename via the API payload:
{ "template": "your-template.docx", "data": { ... } }
